export enum FILTER_CONDITIONS {
    EQUALS ='equals',
    DOES_NOT_EQUAL = 'does_not_equal',
    GREATER_THAN = 'greater_than',
    LESS_THAN = 'less_than',
}

export enum QUESTION_RESPONSE_TYPE {
    DATE_STRING = 'DateString',
    NUMBER_INPUT = 'NumberInput',
}